# Legacy Issue Record: specification evolution

This file preserves the closed issue body extracted from root `HANDOFF.md` at authority-boundary commit `7dea5457828b6590f9ab2a643b58047b032e53d1`. The complete pre-compaction HANDOFF remains recoverable from Git; this record retains the legacy ID and operationally relevant detail after compaction.

### AEP-20260805T095724Z-specification-evolution

- **Status:** `CLOSED`
- **Severity:** `MEDIUM`
- **Owner:** `Codex/root`
- **Authority:** `HUMAN` — at `2026-08-05T09:57:24Z`, the human technical owner explicitly approved the decision-complete plan and exact English policy below for adoption in the reusable specification template.
- **Review:** `INDEPENDENT` — this clarifies the normative evolution contract for the highest-precedence project-truth artifact; a different context-free participant must approve the immutable target before closure.
- **Problem:** The reusable template defines acceptance and change-record mechanics but does not state when specification evolution may be proposed, distinguish evidence from authorization, or explicitly reject implementation drift as a reason to rewrite accepted requirements.
- **Motivation:** Make specification evolution evidence-backed and auditable while retaining human authority, source precedence, implementation-correction defaults, and the existing division between product requirements and architectural decisions.
- **Compatibility:** The proposal adds no truth tier, workflow owner, runtime behavior, dependency, or architecture. It is consistent with the existing `DRAFT`/`ACCEPTED` gate, the seven-level precedence order, the Human Authority Boundary, the rule that mixed product/architecture decisions require both an accepted specification update and accepted ADR, and the existing atomic specification-change record. Root `BOOTSTRAP.md` and root `PROJECT_SPEC.md` remain byte-identical; no ADR is required because no architecture decision changes.
- **Approved proposal:** Add the following section between `Assumptions and open decisions` and `Specification change record`, retaining the existing change-record text and table:

  ```markdown
  ## Specification evolution

  `PROJECT_SPEC.md` is authoritative but not immutable.

  A specification change MAY be proposed when evidence demonstrates that:

  - an existing requirement is ambiguous, incomplete, or internally inconsistent;
  - real-world usage exposes a requirement that was not previously represented;
  - an existing requirement no longer serves the project's stated goals; or
  - a new capability has been explicitly accepted into project scope.

  Evidence supports a proposal; it does not by itself authorize a change to an `ACCEPTED` specification. `PROJECT_SPEC.md` MUST evolve through evidence-backed requirement change, not implementation drift. Existing implementation is not, by itself, sufficient evidence that the specification should change to match it. When implementation conflicts with an `ACCEPTED` `PROJECT_SPEC.md`, the default is to correct the implementation and preserve the conflict record until any specification change is approved.

  Every material requirement change requires explicit human technical-owner approval. This includes changes that materially alter product scope, compatibility, core invariants, authorized architectural constraints, or the product/architecture authority boundary. A change affecting both product requirements and architecture requires both an accepted specification update and a compatible accepted ADR. If they conflict, `PROJECT_SPEC.md` remains authoritative and the conflict remains unresolved.

  Relevant evidence, issue records, and ADRs SHOULD be referenced where appropriate so that the reason for the specification change remains auditable.
  ```
- **Evidence:** Owner-approved plan received before this record; clean synchronized baseline `08a61ee54faedf057a24f1ea1ee970cb21341a79`; root governing hashes `8fcfc3fe52608a1b42305bb12696d3f151be468bbbf638918cf93b651414dfe6` and `13169319e2be028c470ca96925002b25c000c58ba3a4c5420e652d291df139dd`; reusable template pre-change SHA-256 `3bbd5db9c804855848fb66ca7cb68241ce38e717ce0481faa09575e808cf2566`.
- **Implementation target:** Commit `3341c53eb94723e2092bc44a321b111e376d8bef`, parent `428836422d20c2910e790b036e8f3802c53af18c`, full tree `e5291e1ec5a8498f7b677923ca48fcdb2ab00b91`, protocol tree `8676b85d292676b7e198d10414961bf3657bf578`, template blob `de44b6d89f11c695fdfd6f33bb9a8943f78bfba3`, and template SHA-256 `6c7401d9ed598908225f3eb6a3bb8299cfa01a350b1853df98c7bc386eddf976`.
- **Implementation verification:** `git diff --check 428836422d20c2910e790b036e8f3802c53af18c..3341c53eb94723e2092bc44a321b111e376d8bef` exited `0`; `git diff --name-status` reported only `M protocol/PROJECT_SPEC.md`; the semantic/Markdown checker reported `package_inventory=10 symlinks=0 checked_markdown=12 relative_links=28 missing_links=0` and `semantic_assertions=PASS precedence_tiers=7 handoff_sections=5 next_actions=1`; `git diff --quiet` confirmed all non-target package files unchanged; governing hashes matched. Dedicated Markdown linters were unavailable.
- **Resolution state:** Complete and published. Exact approved wording is implemented at immutable target `3341c53eb94723e2092bc44a321b111e376d8bef`, independently `APPROVED` with no material findings, coordinator-verified at review boundary `b3f9f2f29699e6d3650c375b5743f94694bde6e8`, and published through closure commit `e91e608efd6086c2c8aaba341aee448e50f9c0da`. All task lifecycles are terminal.
- **Independent review — `2026-08-05T10:07:36Z` — `agent:codex-specification-evolution-reviewer`:**
  - **Reviewed repository state:** Exact target `3341c53eb94723e2092bc44a321b111e376d8bef`, direct parent/authorization boundary `428836422d20c2910e790b036e8f3802c53af18c`, full target tree `e5291e1ec5a8498f7b677923ca48fcdb2ab00b91`, protocol tree `8676b85d292676b7e198d10414961bf3657bf578`, target template blob `de44b6d89f11c695fdfd6f33bb9a8943f78bfba3`, target template SHA-256 `6c7401d9ed598908225f3eb6a3bb8299cfa01a350b1853df98c7bc386eddf976`, and clean pre-review HANDOFF boundary `e6d3971f8ad91a83a774b6c51585a40d6cb535f3`. Current protocol bytes matched the target before this review record.
  - **Scope:** Complete root `BOOTSTRAP.md`, root `PROJECT_SPEC.md`, current `HANDOFF.md`, reusable `BOOTSTRAP.md` and `PROJECT_SPEC.md`, all other eight files in `protocol/`, exact parent-to-target diff and object modes, owner-authority chronology and exact wording, proposal/evidence/approval semantics, `DRAFT`/`ACCEPTED` gates, seven-tier precedence, implementation-conflict default, universal material-change approval, product/architecture and accepted-ADR rules, existing change-record mechanics, package inventory/symlinks, non-target bytes, Markdown integrity, governing hashes, recorded evidence/limitations, duplication, ambiguity, and alternative interpretations.
  - **Commands or procedures:** `git merge-base --is-ancestor 428836422d20c2910e790b036e8f3802c53af18c 3341c53eb94723e2092bc44a321b111e376d8bef` exited `0`; `git rev-list --parents -n 1 3341c53eb94723e2092bc44a321b111e376d8bef` showed the authorization commit as the sole direct parent. `git show --no-ext-diff --format=fuller --stat 428836422d20c2910e790b036e8f3802c53af18c`, the same command for `3341c53eb94723e2092bc44a321b111e376d8bef`, and `git show 428836422d20c2910e790b036e8f3802c53af18c -- HANDOFF.md` established that the exact proposal, motivation, compatibility, human authority, independent-review gate, and bounded source action were committed before the edit. A process-substitution `diff -u` between the proposal's fenced Markdown and the target section exited `0`. `git diff --stat`, `git diff --name-status`, and `git diff --numstat 428836422d20c2910e790b036e8f3802c53af18c..3341c53eb94723e2092bc44a321b111e376d8bef` reported only `protocol/PROJECT_SPEC.md`, `17` insertions, `0` deletions; `git diff --check 428836422d20c2910e790b036e8f3802c53af18c..3341c53eb94723e2092bc44a321b111e376d8bef` exited `0`; `git diff --quiet 428836422d20c2910e790b036e8f3802c53af18c 3341c53eb94723e2092bc44a321b111e376d8bef -- . ':(exclude)protocol/PROJECT_SPEC.md'` exited `0`, and `git diff --quiet 3341c53eb94723e2092bc44a321b111e376d8bef HEAD -- protocol` exited `0`. `git cat-file`, `git rev-parse`, `git ls-tree -r -l`, and `shasum -a 256` reproduced every commit/tree/blob/hash identity above plus pre-change template SHA-256 `3bbd5db9c804855848fb66ca7cb68241ce38e717ce0481faa09575e808cf2566` and unchanged root hashes `8fcfc3fe52608a1b42305bb12696d3f151be468bbbf638918cf93b651414dfe6` / `13169319e2be028c470ca96925002b25c000c58ba3a4c5420e652d291df139dd`. A read-only Python 3 checker loaded exact target blobs, validated the expected manifest, modes, UTF-8, final newlines, trailing whitespace, fence balance, and fence-aware relative links, and printed `package_inventory=10 regular_blobs=10 symlinks=0`, `checked_markdown=12 ... final_newline_failures=0 trailing_whitespace_failures=0 fence_failures=0`, and `relative_links=28 missing_links=0`. Line-numbered direct reads supplied the semantic review; `markdownlint` and `markdownlint-cli2` were queried and were `NOT_AVAILABLE`.
  - **Specification compliance:** No material finding. The inserted bytes exactly match the owner-approved proposal. Evidence is a prerequisite/support for a proposal, not authorization; every material requirement change is independently owner-gated. The existing `DRAFT` restriction and attributable `ACCEPTED` gate remain intact. The seven truth tiers are byte-unchanged, and only accepted ADRs retain level-2 authority. An accepted-spec/implementation conflict defaults to implementation correction and a preserved conflict record until an approved specification change. Product changes remain in an accepted specification, architecture remains in a compatible accepted ADR, mixed decisions require both, and the specification wins without silently resolving a conflict. The pre-existing change-record paragraph and table are byte-unchanged and still require proposed wording outside authoritative accepted requirements, then an atomic requirement-and-record update after approval. The overlap between the evolution section's authority rule and the adjacent change-record rule is narrow reinforcement, not conflicting ownership: the former states eligibility, evidence, default, and authority boundary; the latter states persistence mechanics.
  - **Correctness and regression findings:** No material finding. Exact parentage proves the authority record preceded the source edit; exact block comparison rules out paraphrase drift. The target is one additive section at the approved location with no mode change, deletion, unrelated byte change, broken link, fence, newline, or whitespace regression. Alternative readings were challenged: “authoritative” does not make a `DRAFT` specification implementation authority because the retained status gate and BOOTSTRAP start procedure control it; an explicitly scoped capability is still not self-authorizing because the universal owner-approval sentence controls every material change; and an ADR can support only compatible architecture, not fill or override a product requirement.
  - **Architecture and complexity findings:** No material finding. The target adds no truth tier, artifact, dependency, state, process, contract, or implementation complexity. It does not adopt an architecture decision, and it preserves the rule that only an `ACCEPTED` ADR is authoritative for architecture; therefore a new ADR is not required for this owner-approved specification-policy clarification.
  - **Material findings and resolution conditions:** `NONE`.
  - **Limitations:** No dedicated Markdown linter was available; the successful standard-library checker is structural and fence-aware, not a complete CommonMark renderer. Root `BOOTSTRAP.md` has a pre-existing final byte `.` rather than LF; it was excluded from the package final-newline pass, remained byte-identical at its governing hash, and is not a target regression. No runtime/product test applies to this Markdown-only policy change. Repository attribution records the human-owner approval with exact text and UTC time but is not cryptographically authenticated. The review did not exercise future adopter decisions or prove participant compliance.
  - **Residual risks:** Terms such as “material,” “evidence,” and “where appropriate” necessarily require case-specific judgment. A future participant could still misclassify a change, but the retained issue, certainty, conflict, human-authority, and independent-review gates provide an explicit escalation path. Markdown cannot mechanically enforce those gates.
  - **Evidence:** Direct immutable Git objects and command results above; authorization and issue history in this HANDOFF; target lines 7–14 and 114–137 of `protocol/PROJECT_SPEC.md`; target lines 13–23, 33–38, 61–64, and 82–128 of `protocol/BOOTSTRAP.md`; complete file inspection rather than implementor-summary reliance.
  - **Disposition:** `APPROVED`.
  - **Prior-round resolution:** `FIRST ROUND`.
